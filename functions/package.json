{
  "name": "mailersend-firebase",
  "description": "MailerSend firebase extension",
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^11.5.0",
    "firebase-functions": "^4.2.0",
    "mailersend": "^2.0.5"
  },
  "devDependencies": {
    "axios": "^1.3.2",
    "chai": "^4.3.7",
    "mocha": "^10.2.0"
  },
  "scripts": {
    "mocha": "mocha '**/*.spec.js'",
    "test": "(cd integration-tests && firebase emulators:exec 'npm run mocha' -P demo-test)",
    "generate-readme": "firebase ext:info .. --markdown > ../README.md",
    "serve": "cd integration-tests && firebase emulators:start --project=demo-test",
    "lint": "eslint"
  },
  "private": true
}
